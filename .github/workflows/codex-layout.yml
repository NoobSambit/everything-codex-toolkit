name: Codex Layout

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  layout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Check Codex layout
        run: bash scripts/check-codex-layout.sh
